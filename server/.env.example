# Copy to server/.env (or export these) to enable real Web Push.
# Generate a key pair with:  npm run gen-vapid
PORT=8080
VAPID_SUBJECT=mailto:you@example.com
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
# Optional: require this token (X-Admin-Token header) to call /api/notify
ADMIN_TOKEN=
